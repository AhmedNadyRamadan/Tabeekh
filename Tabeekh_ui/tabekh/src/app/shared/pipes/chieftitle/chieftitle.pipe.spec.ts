import { chieftitlePipe } from './chieftitle.pipe';

describe('chieftitlePipe', () => {
  it('create an instance', () => {
    const pipe = new chieftitlePipe();
    expect(pipe).toBeTruthy();
  });
});
